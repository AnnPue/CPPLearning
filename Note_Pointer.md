# 指针

## 2025.12.22

指针是全局的

&：取变量首字节的地址

指针大小与指向的数据类型无关

32位：任何类型的指针都是4个字节（8位16进制数）；64位：8个字节（16位16进制数）

p + n == p + n * sizeof(type)

常量指针：
~~~
int n[10];  // n == &n[0]
~~~
变量指针：
~~~
int *p;
p = n;  // p == &n[0]
p++;    // *p == n[1]
~~~
~~~
char *massage1 = "How are you? ";   // 变量指针
char massage2[] = "How are you? ";  // 常量指针

massage1++; // *massage1 == 'o'
~~~

~~~
void(*sp)(int, int);    // 函数指针：将一个操作作为参数传递
sp = swap;
sp(a, b);
~~~
指向常量的指针：const int *p，可以改变指向，不能通过指针修改值


指针常量：不可以改变指向，能通过指针修改值
```
int a = 10;
int *const p = &a;
```
## 2025.12.29

全局数组的大小不能是const常量，可以是define或文字常量

多维数组：行优先策略：arr[0][0], arr[0][1], ... , arr[1][0], arr[1][1], ...

初始化：arr[][10]. 第一维可以省略
```
arr[2][4] = {
    {1,2,3,4}
    {5,6.7.8}
}
```
在函数里调用：arr

二维数组只写一个维度时是地址：arr[3] == &arr[3][0]

### typedef
~~~
typedef int ARR3D[10][20][30];

ARR3D arr1, arr2;
/*
即
int arr1[10][20][30];
int arr2[10][20][30];
*/
~~~
```
int func(int a, int b, int c);
p = func;
int (*p1)(int a, int b, int c);
int (*p2)(int a, int b, int c);
int (*p3)(int a, int b, int c);


typedef int (*FUNC_POINTER)(int a, int b, int c);
FUNC_POINTER p1,p2,p3;
```

### struct

struct定义的叫对象，里面的内容叫成员

浅copy：**同类型**之间等号赋值的默认赋值方式，将一个对象的各成员的值赋值给另一个对象

浅拷贝会创建一个全新的外层容器对象（新内存地址，独立内存），但是新容器中的所有元素，都会直接复用原容器元素的内存地址 → 新 / 原容器的内部元素，全部共享内存。

共享内存：两个变量 / 对象，同时指向内存中的「同一块地址空间」，这块内存里的数据，对这两个变量来说是「共用」的 → 这就是共享内存。

联合体（union）：只有最近一次被赋值的是有效的。可以包含多个不同类型的成员变量，但这些成员变量会共同占用同一块连续的**内存空间**，同一时间，只有一个成员变量能真正生效使用这块内存。

指针指向结构体对象，访问其成员：->

### 申请内存

C: malloc / C++: new

释放：free / delete

使用malloc申请的内存不free则“永生”，除非物理释放（关机）

## 2026.01.05

二维数组指针+1，相当于跳过一行