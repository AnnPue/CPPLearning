# 函数

## 2025.11.17

定义：函数头+函数体，定义同时也是声明。

声明：例如：

~~~
int Cube(int);
~~~

函数可以声明多次，但只能定义一次。

函数原型是声明而非定义。

嵌套：父子关系。并列：兄弟关系

所有的函数都是兄弟关系。

**习惯**函数声明放在main前，函数定义放在main后。

函数重载：函数名称相同，但参数列表（参数个数或参数类型）不同。

形式参数：formal parameter，实参：argument。

**同一个作用域**中不能有标识符重名。

call：调用函数，需要传递参数。

仅传递变量无法修改变量，函数只是利用了变量里的值。传递**指针**可以在函数里修改实参。

---
xx.h文件：头文件，存放函数声明

xx.c文件：存放函数定义。

main.c：#include "xx.h"   可以调用其他文件里的函数

## 2025.12.08

int a[] == int *a

string s == char *s

函数传递参数：传递数组时只能传递地址，所以传递的数组和原数组是同一个，在函数中可以操作原数组

函数在注释里写前态和后态。

前态：调用函数需要满足的条件

后态：满足前态的条件下，调用函数会发生什么

指针是全局的，函数是全局的，全局常量推荐用，全局变量副作用多，别用
